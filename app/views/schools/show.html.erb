<%= link_to "Back", schools_path %>
<h1>
<%= @school.title %>
<%= @school.address %>
</h1>
<p>
<%= @school.content %>
</p>
<% @school.teachers.each do |teacher| %>
	<li>
		<p><%= teacher.name%></p>
		<p><%= teacher.description%></p>
	</li>
<% end %>